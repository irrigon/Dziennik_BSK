@page
@model Dziennik_BSK.Pages.Account.Manage.Users.IndexModel

@{
    ViewData["Title"] = "Konta";
}

<h2>Konta</h2>

<p>
    <a asp-page="Create">Dodaj nowe konto</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ApplicationUser[0].Role)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ApplicationUser[0].UserName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ApplicationUser[0].Email)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ApplicationUser) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Role)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edytuj</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Szczegó³y</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Usuñ</a>
            </td>
        </tr>
}
    </tbody>
</table>
